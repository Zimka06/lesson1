#include <stdio.h>
#include <iostream>
#include <math.h>
int maxCh(int* arr, int n);
int SeriyMax(int* arr, int n);
int main()
{
	int cnt = 0;
	int c = 0;
	int x = 0;
	const int n = 4;
	const int m = 10;
	int arr1[m] = { 1,1,0,2,4,4,4,4,4 };
	int arr2[n][n] = {1,2,3,4,
					  8,8,8,8,
					  9,9,0,9,
					  7,7,6,0 };
	for (int j = 0; j < n; j++)
	{
		x = 0;
		for (int i = 0; i < n; i++)
			if (arr2[i][j] == 0)
				x++;
		if (x>0)c++;
	}
	printf("kolichestvo 0 stolbcov - %d",c);
	int f = SeriyMax(arr1, m);
	printf("\nmax seriy  - %d", f);// 3 variant

	int g = maxCh(arr1, m);
	printf("\nmax for array - %d", g);
}
int maxCh(int* arr,int n)
{
	int max1 = 0;
	for (int i = 0; i < n; i++)
	{
		if (arr[i] > max1)max1 = arr[i];
	}
	return max1;
}
int SeriyMax(int* arr, int n) {
	int l = 0;
	int k = 0;
	int max = 0;
	for (l; l < n; l++)
	{
		if (arr[l] == arr[l + 1]) k++;
		else
		{
			if (k > max)max = k;
		}
	}
	return max;
}
